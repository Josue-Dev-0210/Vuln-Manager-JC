# Vuln-manager

Aplicación Flask para gestionar vulnerabilidades localmente (SQLite).

Ver `app.py` para endpoints y `static/main.js` para frontend.

Ejecutar local:

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt  # si existe
python3 app.py